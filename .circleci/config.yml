version: 2.1
jobs:
  build:
    docker:
      - image: iatapay/maven-java11-docker-aws-cli
    steps:
      - checkout
      - run:
          name: Build
          command: mvn -B -DskipTests clean package
	  - run: aws s3 sync ./target/*.war s3://ci-cd-example-2022
